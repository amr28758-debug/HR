{
  "name": "@burtplace/integration-zoho",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LEGACY, REMOVABLE. Maps Zoho People exports to Burtplace migration rows. Nothing in core depends on this package.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "test": "vitest run", "lint": "echo ok" },
  "devDependencies": { "vitest": "^2.1.8" }
}
